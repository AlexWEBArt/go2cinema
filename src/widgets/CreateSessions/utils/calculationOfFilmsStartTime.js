export default function calculationOfFilmsStartTime(startSeances) {
    // const width = 
    // const background = 
    const startTime = startSeances.slice(0, 2) * 60 + startSeances.slice(2, 4)
    console.log(startSeances.slice(2, 4))
    // const position = 30 * hours
    return startTime
}